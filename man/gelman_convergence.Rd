% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/samplingmethods_pt1.R
\name{gelman_convergence}
\alias{gelman_convergence}
\title{Run Multiple MCMC Chains and Assess Convergence}
\usage{
gelman_convergence(
  sampler,
  inits_list,
  burn_in = 1000,
  thinning = 30,
  return_samples = FALSE,
  ...
)
}
\arguments{
\item{sampler}{A function that performs MCMC sampling. Must accept initial values
as its first argument and return a matrix of samples.}

\item{inits_list}{A list of initial value vectors (one for each chain)}

\item{burn_in}{Number of initial samples to discard (default: 1000)}

\item{thinning}{Keep only every k-th sample (default: 30)}

\item{...}{Additional arguments passed to the sampler function}

\item{n_iter}{Number of iterations per chain}
}
\value{
A list containing:
\itemize{
\item{samples: List of thinned samples from each chain}
\item{gelman_diag: Results from gelman.diag()}
\item{gelman_plot: The Gelman-Rubin plot}
}
}
\description{
Runs multiple chains of an MCMC sampler with different starting values and
produces convergence diagnostics including Gelman-Rubin statistics and plots.
}
\examples{
# Example using the metropolis() function from earlier
target <- function(x) exp(-0.5 * sum(x^2)) / (2*pi)
proposal_sampler <- function(n, current) {
  matrix(rnorm(n*2, mean = 0, sd = 0.5), ncol = 2)
}

results <- run_convergence_diagnostics(
  sampler = metropolis,
  inits_list = list(c(0,0), c(2,2), c(-1,1)),
  n_iter = 10000,
  target = target,
  proposal_sampler = proposal_sampler
)

# View results
print(results$gelman_diag)
plot(results$gelman_plot)
}
