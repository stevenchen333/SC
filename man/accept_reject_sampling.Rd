% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/samplingmethods_pt1.R
\name{accept_reject_sampling}
\alias{accept_reject_sampling}
\title{Accept-Reject Sampling}
\usage{
accept_reject_sampling(proposal_pdf, target_pdf, proposal_sample, M, n)

accept_reject_sampling(proposal_pdf, target_pdf, proposal_sample, M, n)
}
\arguments{
\item{proposal_pdf}{A function representing the probability density function (PDF) of the proposal distribution.}

\item{target_pdf}{A function representing the probability density function (PDF) of the target distribution.}

\item{proposal_sample}{A function that generates random samples from the proposal distribution.}

\item{M}{A constant such that target_pdf(x) <= M * proposal_pdf(x) for all x.}

\item{n}{The number of samples to generate.}
}
\value{
A numeric vector of accepted samples.

A list containing:
\itemize{
\item \code{samples}: A numeric vector of accepted samples.
\item \code{theoretical_efficiency}: Theoretical efficiency (acceptance probability).
\item \code{empirical_efficiency}: Empirical efficiency (based on actual samples).
}
}
\description{
This function generates samples from a target distribution using
the accept-reject sampling method.

This function generates samples from a target distribution using
the accept-reject sampling method and calculates both the \strong{theoretical}
and \strong{empirical efficiency}.
}
\examples{
proposal_pdf <- function(x) { ifelse(x >= -3 & x <= 3, 1/6, 0) }  # Uniform(-3,3) PDF
target_pdf <- function(x) { dnorm(x, mean = 0, sd = 1) }  # Standard normal PDF
M <- max(dnorm(seq(-3,3,length.out=100)) / proposal_pdf(seq(-3,3,length.out=100)))
samples <- accept_reject_sampling(proposal_pdf, target_pdf, function(n) runif(n, -3, 3), M, 1000)
plot_sampling(samples, target_pdf)
proposal_pdf <- function(x) { ifelse(x >= -3 & x <= 3, 1/6, 0) }  # Uniform(-3,3) PDF
target_pdf <- function(x) { dnorm(x, mean = 0, sd = 1) }  # Standard normal PDF
M <- max(dnorm(seq(-3,3,length.out=100)) / proposal_pdf(seq(-3,3,length.out=100)))
result <- accept_reject_sampling(proposal_pdf, target_pdf, function(n) runif(n, -3, 3), M, 1000)
plot_sampling(result$samples, target_pdf)
result$theoretical_efficiency
result$empirical_efficiency
}
