% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/samplingmethods_pt1.R
\name{plot_2d_pdf}
\alias{plot_2d_pdf}
\title{Visualize 2D Target PDF and Sampled Data}
\usage{
plot_2d_pdf(
  target_pdf,
  sampled_data,
  xlim,
  ylim,
  n_grid = 100,
  contour_bins = 15,
  point_alpha = 0.5,
  point_size = 1,
  fill_contours = TRUE,
  show_points = TRUE
)
}
\arguments{
\item{target_pdf}{A function that takes \code{x} and \code{y} as inputs and returns the PDF value at \verb{(x,y)}.}

\item{sampled_data}{A \code{data.frame} or \code{matrix} with 2 columns (x, y) of sampled points.}

\item{xlim}{Numeric vector of length 2 (\code{c(min, max)}) for the x-axis range.}

\item{ylim}{Numeric vector of length 2 (\code{c(min, max)}) for the y-axis range.}

\item{n_grid}{Number of grid points along each axis for evaluating the PDF (default: \code{100}).}

\item{contour_bins}{Number of contour levels (default: \code{15}).}

\item{point_alpha}{Transparency of sampled points (0 = fully transparent, 1 = opaque, default: \code{0.5}).}

\item{point_size}{Size of sampled points (default: \code{1}).}

\item{fill_contours}{If \code{TRUE}, fills contours with a gradient (default: \code{FALSE}).}

\item{show_points}{If \code{FALSE}, hides the sampled points (default: \code{TRUE}).}
}
\value{
A \code{ggplot2} plot object.
}
\description{
Creates a contour plot of a 2D target probability density function (PDF)
overlaid with a scatter plot of sampled data points.
}
\examples{
# Example 1: Bivariate normal
target_normal <- function(x, y) {
  exp(-0.5 * (x^2 + y^2)) / (2 * pi)
}
sampled_data <- MASS::mvrnorm(1000, mu = c(0, 0), Sigma = diag(2))
plot_2d_pdf(target_normal, sampled_data, xlim = c(-3, 3), ylim = c(-3, 3))

# Example 2: Custom PDF (like yours)
target_custom <- function(x, y) {
  ifelse(x > -1 & y > 0, y * exp(-y * (x + 1)), 0)
}
sampled_data <- data.frame(x = rexp(1000) - 1, y = rexp(1000))  # Not true samples!
plot_2d_pdf(target_custom, sampled_data, xlim = c(-1, 5), ylim = c(0, 5))

}
