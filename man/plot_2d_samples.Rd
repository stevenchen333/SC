% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/samplingmethods_pt1.R
\name{plot_2d_samples}
\alias{plot_2d_samples}
\title{Plot 2D Target PDF and Sampled Data}
\usage{
plot_2d_samples(
  target_pdf,
  sampled_data,
  xlim,
  ylim,
  n_grid = 100,
  contour_bins = 15,
  point_alpha = 0.5,
  point_size = 1
)
}
\arguments{
\item{target_pdf}{A function that takes a 2-column matrix of (x,y) coordinates
and returns the PDF values at those points.}

\item{sampled_data}{A matrix or data.frame with 2 columns containing the
sampled (x,y) points.}

\item{xlim}{Numeric vector of length 2 specifying the x-axis limits (min, max).}

\item{ylim}{Numeric vector of length 2 specifying the y-axis limits (min, max).}

\item{n_grid}{Integer specifying the number of grid points in each dimension
for evaluating the target PDF (default = 100).}

\item{contour_bins}{Integer specifying the number of contour levels to show
(default = 15).}

\item{point_alpha}{Numeric (0-1) specifying transparency of sampled points
(default = 0.5).}

\item{point_size}{Numeric specifying size of sampled points (default = 1).}
}
\value{
A ggplot object showing the target PDF as contours and sampled data as points.
}
\description{
Creates a visualization comparing a 2D target probability density function (PDF)
with sampled data points using ggplot2. The target PDF is shown as contour lines
and the sampled data as points.
}
\examples{
# Example with bivariate normal distribution
target_pdf <- function(x) mvtnorm::dmvnorm(x, mean = c(0,0), sigma = diag(2))
sampled_data <- mvtnorm::rmvnorm(1000, mean = c(0,0), sigma = diag(2))
plot_pdf_samples(target_pdf, sampled_data, xlim = c(-3,3), ylim = c(-3,3))

}
